{
  "name": "portfolio-render-entrypoint",
  "version": "1.0.0",
  "private": true,
  "description": "Root entrypoint for Render deployments. The API lives in backend-node.",
  "scripts": {
    "postinstall": "npm --prefix backend-node ci",
    "start": "npm --prefix backend-node start"
  },
  "engines": {
    "node": ">=18"
  }
}
